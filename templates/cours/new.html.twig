{% extends 'base.html.twig' %}

{% block title %}New Cours{% endblock %}

{% block body %}
<body class="no-trans">
  <div class="container">
    <div class="row">					 
     
    <h1>Create new Cours</h1>

    {{ include('cours/_form.html.twig') }}

    
    {% if is_granted("ROLE_USER") %}
    <button href="{{ path('cours_index') }}" class="btn btn-sm btn-default">En savoir plus &rarr;</button>
    {% else %}
    <button href="{{ url('app_login') }}" class="btn btn-sm btn-default">En savoir plus &rarr;</button>
    {% endif %}



   

</div>
</div>
</body>
{% endblock %}

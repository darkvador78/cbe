<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        
    <link rel="stylesheet" href="https://bootswatch.com/4/flatly/bootstrap.css" media="screen">
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.12.1/css/all.min.css" integrity="sha256-mmgLkCYLUQbXn0B1SRqzHar6dCnv9oZFPEC1g1cwlkk=" crossorigin="anonymous" />    </head>

        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}{% endblock %}
    </head>
    <body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
            <a class="navbar-brand" href="{{ url('home') }}">
                <img src="{{ asset('img/logo.png') }}" alt="P A C" class="logo-base img-fluid" style="width:75px;">               
                {# <img src="{{ asset('img/') }}logo.png" alt="P*A*C">            methode 2 #}
                {# <img src="{{ asset('imag/') ~ 'logo.png'}}" alt="P*A*C">        methode 3 #}
            </a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor02" aria-controls="navbarColor02" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarColor02">
                <ul class="navbar-nav mr-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="{{ url('home') }}">Home</a>
                    </li>
                    {# <li class="nav-item">
                        <a class="nav-link" href="{{ url('blog') }}">Blog</a>
                    </li> #}
                    {# {% if is_granted('ROLE_ADMIN') %} #}
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Admin</a>
                        <div class="dropdown-menu" style="">
                        <a class="dropdown-item disabled" href="#">Admin Cours</a>
                        <a class="dropdown-item" href="{{ path('cours_index') }}">Listing</a>
                        <a class="dropdown-item" href="{{ path('cours_new') }}">New</a>
                        {# <div class="dropdown-divider"></div>
                        <a class="dropdown-item disabled" href="{{ path('post') }}">Admin Tag</a>
                        <a class="dropdown-item" href="{{ path('tag_index') }}">Listing</a>
                        <a class="dropdown-item" href="{{ path('tag_new') }}">New</a>
                        <div class="dropdown-divider"></div>
                        <a class="dropdown-item disabled" href="{{ path('post') }}">Admin Category</a>
                        <a class="dropdown-item" href="{{ path('category_index') }}">Listing</a>
                        <a class="dropdown-item" href="{{ path('category_new') }}">New</a>
                        <div class="dropdown-divider"></div>
                        <a class="dropdown-item disabled" href="{{ path('post') }}">Admin User</a>
                        <a class="dropdown-item" href="{{ path('user_index') }}">Listing</a>
                        <a class="dropdown-item" href="{{ path('user_new') }}">New</a>
                        </div>
                    </li> #}
                    {# {% endif %} #}
                    {# <li>
                    {% if is_granted('ROLE_USER') %}
                        <a class="dropdown-item" href="{{ path('app_logout') }}">Logout</a>
                    {% else %}    
                        <a class="dropdown-item" href="{{ path('app_login') }}">Login</a>
                    {% endif %}
                    </li> #}
                </ul>
            </div>
        </nav>

        {% block body %}{% endblock %}
                <script src="https://code.jquery.com/jquery-3.4.1.min.js" crossorigin="anonymous"></script>
        <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.0/dist/umd/popper.min.js" integrity="sha384-Q6E9RHvbIyZFJoft+2mJbHaEWldlvI9IOYy5n3zV9zzTtmI3UksdQRVvoxMfooAo" crossorigin="anonymous"></script>
        <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/js/bootstrap.min.js" integrity="sha384-wfSDF2E50Y2D1uUdj0O3uMBJnjuUD4Ih7YwaYd1iqfktj0Uod8GCExl3Og8ifwB6" crossorigin="anonymous"></script>

        {% block javascripts %}{% endblock %}
    </body>
</html>
